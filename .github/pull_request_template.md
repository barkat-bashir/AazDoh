## 📋 Description of Changes
A concise summary of the changes introduced in this pull request and the rationales behind them.

---

## 🧩 Affected Components
- [ ] Backend (`backend/`)
- [ ] Frontend SPA (`frontend/`)
- [ ] Terminal CLI (`cli/` - `aazdoh-cli`)
- [ ] Model Context Protocol Server (`mcp-server/` - `aazdoh-mcp`)
- [ ] Documentation / CI (`README.md`, `.github/`)

---

## 🧪 Verification & Testing
Describe how you tested your changes:
- [ ] Backend tests passing (`./mvnw test`)
- [ ] Frontend build succeeded with 0 TypeScript errors (`npm run build`)
- [ ] CLI build succeeded (`npm run build` in `cli/`)
- [ ] MCP Server build succeeded (`npm run build` in `mcp-server/`)
- [ ] Manual verification steps performed (e.g. verified on local dev server `localhost:3000` / CLI command execution)

---

## 📸 Screenshots / Terminal Logs (if applicable)
Add before/after screenshots or terminal receipts here.

---

## 🛡️ Checklist
- [ ] My code adheres to the project's code style and formatting standards.
- [ ] I have updated relevant documentation if my changes modify existing behavior.
- [ ] No sensitive credentials, tokens, or environment keys are committed.
